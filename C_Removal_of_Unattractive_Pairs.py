testcase = int(input())

for _ in range(testcase):
    n = int(input())
    s = list(input())

    # for every two element;
    #    check if they are the same or check if the next two elements are the same:
    #         if true delete the between and delete the other

    

    

    print(len(s))
        


 